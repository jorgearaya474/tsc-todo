# TypeScript Shell Todo List

Shell app to manage todos with typescript and persistent data with lowdb.

## Installation
- run `npm install`.
- run `tsc` to compile the typescript code
- run `node dist/index.js` to run the app

___
